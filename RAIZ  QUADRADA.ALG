algoritmo "Raiz  quadrada"
// Disciplina   : [Linguagem e Lógica de Programação] 
// Professor   : Antonio Carlos Nicolodi 
var

resul , n : Real
inicio
        escreval ( " Digite um Numero para calcular  raiz quadrada")
        leia (n)
          // resul <- Raizq(n)
        resul <- int (raizq (n))  // exibe somente o Numero inteir0
        escreva(" a raiz quadrada é " , resul)
fimalgoritmo